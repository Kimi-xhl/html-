// JavaScript HTML DOM
/**
 * HTML DOM 文档对象模型 Document Object Model
 */

// 1. 改变页面中所有的HTML 元素
// 2. 改变页面中所有HTML 元素的属性
// 3. 改变页面中所有css样式
// 4. 对页面中的事件做出反应

/**
 * 操作html元素 为了做到 首先必须先找到元素 
 *  
 *  通过ID 找到HTML元素
 *  通过标签名 找到HTML元素
 *  ...
 */

// <img id="sss" alt=""></img>

